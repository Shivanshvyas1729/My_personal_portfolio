[
  {
    "id": "vit",
    "title": "Vision Transformer",
    "primary_category": "Computer Vision",
    "aliases": ["ViT", "Vision Transformer (ViT)"],
    "secondary_tags": ["Classification", "Transformer", "Self-Attention"],
    "definition": "A transformer architecture applied directly to images by splitting them into fixed-size patches (16x16 or 32x32 pixels), flattening them into sequences, and treating each patch as a 'token'. Self-attention across patches replaces convolutions.",
    "why_used": "When trained on large datasets, ViT matches or outperforms CNNs like ResNet. Its attention mechanism captures global relationships between image regions from the start, unlike CNNs which build global context layer by layer.",
    "interview_point": "ViT's key advantage over CNNs: self-attention can capture long-range dependencies between distant image patches in a single layer. CNNs need many layers to achieve global context. Limitation: ViT needs much more data (JFT-300M) than CNNs — it doesn't have the inductive bias (translation invariance, locality) that CNNs exploit.",
    "advantages": ["Captures global context efficiently", "Scales better than CNNs with more data", "Strong pretrained models available", "Foundation for multimodal models"],
    "limitations": ["Requires large amounts of data to outperform CNNs", "Higher computational cost for small images", "Less data efficient than CNNs"],
    "use_cases": ["Large-scale image classification", "Medical image analysis", "Multimodal vision-language models"],
    "related_terms": ["ResNet", "EfficientNet", "CLIP", "DINO"],
    "real_world_example": "```python\nimport torchvision.models as models\nimport torch.nn as nn\n\n# Load pretrained ViT-B/16\nmodel = models.vit_b_16(weights='IMAGENET1K_V1')\n\n# Fine-tune for custom classification\nnum_features = model.heads.head.in_features\nmodel.heads.head = nn.Linear(num_features, num_classes)\n\n# ViT expects 224x224 images normalized with ImageNet stats\nfrom torchvision import transforms\npreprocess = transforms.Compose([\n    transforms.Resize(256),\n    transforms.CenterCrop(224),\n    transforms.ToTensor(),\n    transforms.Normalize(mean=[0.485, 0.456, 0.406],\n                         std=[0.229, 0.224, 0.225])\n])\n```",
    "difficulty_level": "Advanced",
    "term_type": "Architecture"
  },
  {
    "id": "segformer",
    "title": "SegFormer",
    "primary_category": "Computer Vision",
    "aliases": ["SegFormer Architecture"],
    "secondary_tags": ["Segmentation", "Transformer", "Semantic Segmentation"],
    "definition": "A transformer-based semantic segmentation model that combines a hierarchical transformer encoder with a lightweight MLP decoder. Avoids positional encodings (scale-agnostic) and uses overlapping patch merging to capture local continuity.",
    "why_used": "State-of-the-art semantic segmentation with simpler architecture than previous models. Efficient at multiple scales (B0-B5 variants) and outperforms CNN-based segmentation on major benchmarks.",
    "interview_point": "SegFormer's key innovations: 1) Mix Transformer (MiT) encoder with overlapping patch merging — captures local continuity between patches. 2) No positional encodings — uses 2D average pooling to handle any resolution without retraining. 3) Lightweight all-MLP decoder — fuses multi-scale features simply but effectively.",
    "advantages": ["State-of-the-art segmentation quality", "Efficient multi-scale variants (B0-B5)", "No positional encodings — flexible resolution", "Simpler decoder than previous methods"],
    "limitations": ["Less interpretable than U-Net skip connections", "Requires GPU for efficient training"],
    "use_cases": ["Autonomous driving scene understanding", "Medical image segmentation", "Satellite image analysis"],
    "related_terms": ["U-Net", "DeepLab", "Semantic Segmentation"],
    "real_world_example": "```python\nfrom transformers import SegformerForSemanticSegmentation, SegformerImageProcessor\nfrom PIL import Image\nimport torch\n\nprocessor = SegformerImageProcessor.from_pretrained('nvidia/segformer-b2-finetuned-cityscapes-1024-1024')\nmodel = SegformerForSemanticSegmentation.from_pretrained('nvidia/segformer-b2-finetuned-cityscapes-1024-1024')\n\n# Inference\nimage = Image.open('street.jpg')\ninputs = processor(images=image, return_tensors='pt')\n\nwith torch.no_grad():\n    outputs = model(**inputs)\n\n# Get segmentation map\nlogits = outputs.logits  # Shape: (batch_size, num_labels, H/4, W/4)\npredicted_map = logits.argmax(dim=1).squeeze().numpy()\n```",
    "difficulty_level": "Advanced",
    "term_type": "Architecture"
  },
  {
    "id": "easyocr",
    "title": "EasyOCR",
    "primary_category": "Computer Vision",
    "aliases": ["Easy OCR", "EasyOCR Library"],
    "secondary_tags": ["OCR", "Text Detection", "Text Recognition"],
    "definition": "A Python library for Optical Character Recognition (OCR) supporting 80+ languages. Uses a two-stage pipeline: CRAFT (Character Region Awareness for Text detection) for text detection + CRNN (Convolutional Recurrent Neural Network) for text recognition.",
    "why_used": "Simplest OCR solution with a single pip install and 2-line inference. Supports multilingual text, handwriting, and natural scene text out of the box.",
    "interview_point": "EasyOCR pipeline: 1) Detection: CRAFT network finds text regions as bounding boxes. 2) Recognition: CRNN (CNN features + BiLSTM + CTC loss) reads the text in each bounding box. CTC (Connectionist Temporal Classification) loss is used because the output length varies from input length.",
    "advantages": ["80+ languages", "Simple 2-line API", "Works on natural scene text", "CPU and GPU support"],
    "limitations": ["Slower than commercial OCR (Google Vision API)", "Less accurate on complex layouts", "Limited document structure understanding"],
    "use_cases": ["Document digitization", "License plate recognition", "Receipt and invoice processing", "Multilingual text extraction"],
    "related_terms": ["PaddleOCR", "Tesseract", "Document AI"],
    "real_world_example": "```python\nimport easyocr\nfrom PIL import Image\nimport numpy as np\n\n# Initialize reader (downloads models on first run)\nreader = easyocr.Reader(\n    ['en', 'hi'],   # Languages to detect\n    gpu=True        # Use GPU if available\n)\n\n# Read text from image\nresults = reader.readtext(\n    'document.jpg',\n    detail=1,       # Return bounding boxes and confidence\n    paragraph=True  # Merge text into paragraphs\n)\n\n# Each result: (bbox, text, confidence)\nfor bbox, text, confidence in results:\n    print(f'Text: \"{text}\" | Confidence: {confidence:.3f}')\n\n# Quick single-line extraction\ntexts = reader.readtext('image.jpg', detail=0)\nfull_text = ' '.join(texts)\n```",
    "difficulty_level": "Beginner",
    "term_type": "Tool"
  },
  {
    "id": "deeplabv3",
    "title": "DeepLabV3+",
    "primary_category": "Computer Vision",
    "aliases": ["DeepLab", "DeepLabV3Plus"],
    "secondary_tags": ["Segmentation", "Semantic Segmentation", "Atrous Convolution"],
    "definition": "A semantic segmentation CNN by Google that uses atrous (dilated) convolutions to capture multi-scale context without losing resolution. DeepLabV3+ adds an encoder-decoder structure with skip connections for sharper boundaries.",
    "why_used": "State-of-the-art semantic segmentation with excellent boundary detail. Atrous convolutions efficiently enlarge the receptive field without increasing parameters or reducing resolution.",
    "interview_point": "Atrous (dilated) convolutions: insert zeros (holes) between convolution kernel elements to increase receptive field without increasing parameters. Atrous Spatial Pyramid Pooling (ASPP) applies multiple atrous convolutions with different rates to capture multi-scale context in parallel.",
    "advantages": ["Excellent boundary detail via encoder-decoder", "Multi-scale context via ASPP", "Strong performance on standard benchmarks (PASCAL VOC, Cityscapes)"],
    "limitations": ["Computationally heavier than simpler segmentation models", "Requires labeled pixel-level masks for training"],
    "use_cases": ["Scene understanding", "Autonomous driving", "Medical image segmentation", "Satellite image analysis"],
    "related_terms": ["U-Net", "SegFormer", "Dilated Convolutions", "ASPP"],
    "real_world_example": "```python\nimport torch\nfrom torchvision.models.segmentation import deeplabv3_resnet101\n\n# Load pretrained DeepLabV3\nmodel = deeplabv3_resnet101(weights='DEFAULT')\nmodel.eval()\n\n# Preprocess image\nfrom torchvision import transforms\ntransform = transforms.Compose([\n    transforms.ToTensor(),\n    transforms.Normalize(mean=[0.485, 0.456, 0.406],\n                         std=[0.229, 0.224, 0.225])\n])\n\nimage_tensor = transform(image).unsqueeze(0)\n\nwith torch.no_grad():\n    output = model(image_tensor)['out']\n\n# Get segmentation map\nseg_map = output.argmax(dim=1).squeeze().numpy()\n```",
    "difficulty_level": "Advanced",
    "term_type": "Architecture"
  },
  {
    "id": "rgb_conversion",
    "title": "RGB Conversion",
    "primary_category": "Computer Vision",
    "aliases": ["RGB conversion", "RGB", "BGR to RGB"],
    "secondary_tags": ["Preprocessing", "Image Processing"],
    "definition": "The process of converting an image from one color space (like BGR or Grayscale) to the RGB (Red, Green, Blue) color space, which is the standard input format expected by most deep learning models.",
    "why_used": "OpenCV loads images in BGR format by default, while libraries like PyTorch, TensorFlow, and PIL expect RGB. Incorrect color spaces lead to degraded model predictions.",
    "interview_point": "In computer vision pipelines, loading images with OpenCV (`cv2.imread`) returns BGR arrays. Failing to convert them to RGB via `cv2.cvtColor(img, cv2.COLOR_BGR2RGB)` before passing them to a deep learning model (e.g. ResNet) will result in swapped channels (Red and Blue) and poor performance.",
    "advantages": ["Ensures color channel alignment with pretraining", "Standardizes input format"],
    "limitations": ["Adds minor overhead in high-throughput preprocessing pipelines"],
    "use_cases": ["Deep Learning image preprocessing", "Image visualization"],
    "related_terms": ["BGR", "Grayscale", "Normalization"],
    "real_world_example": "```python\nimport cv2\n\nimage = cv2.imread('nodule.jpg')  # Loads as BGR\nrgb_image = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)\n# Now ready for deep learning model ingestion\n```",
    "difficulty_level": "Beginner",
    "term_type": "Technique"
  },
  {
    "id": "image_resizing",
    "title": "Image Resizing",
    "primary_category": "Computer Vision",
    "aliases": ["Image resizing to 224×224 pixels", "Image Resizing", "Resize"],
    "secondary_tags": ["Preprocessing", "Image Processing"],
    "definition": "Adjusting the spatial dimensions (width and height) of an input image to match the fixed input resolution required by a neural network backbone.",
    "why_used": "Fully connected classification layers in CNNs require fixed-size input tensors. Standardizing image dimensions allows batching inputs together.",
    "interview_point": "Resizing can be done using different interpolation methods: Bilinear (fast, standard), Bicubic (higher quality, slower), or Nearest Neighbor (preferred for segmentation masks to avoid generating new class values). When resizing, maintaining aspect ratio with padding is often preferred to avoid warping objects.",
    "advantages": ["Standardizes tensor dimensions for batching", "Reduces memory usage by shrinking large inputs"],
    "limitations": ["Can warp image geometry if aspect ratio is ignored"],
    "use_cases": ["Image classification pipelines", "Input preparation for CNN backbones"],
    "related_terms": ["Interpolation", "Aspect Ratio", "Padding"],
    "real_world_example": "```python\nimport cv2\n\n# Resize using Bilinear interpolation to 224x224\nresized = cv2.resize(image, (224, 224), interpolation=cv2.INTER_LINEAR)\n```",
    "difficulty_level": "Beginner",
    "term_type": "Technique"
  },
  {
    "id": "pixel_normalization",
    "title": "Pixel Normalization",
    "primary_category": "Computer Vision",
    "aliases": ["Pixel normalization to the range [0,1]", "Pixel Normalization", "Min-Max Normalization"],
    "secondary_tags": ["Preprocessing", "Image Processing"],
    "definition": "Scaling pixel values from their raw intensity range (typically 0-255 for 8-bit images) to a standardized range (like 0 to 1 or -1 to 1).",
    "why_used": "Improves gradient descent stability during training by keeping input features in a small, uniform range. Prevents exploding/vanishing gradients.",
    "interview_point": "Standard image normalization: `x / 255.0` scales values to `[0,1]`. Often followed by Channel-wise Standardization: subtracting the dataset mean and dividing by the standard deviation (e.g. ImageNet stats: `mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]`).",
    "advantages": ["Accelerates model training convergence", "Improves numerical stability"],
    "limitations": ["Requires consistent scale between training and inference"],
    "use_cases": ["Image preprocessing for deep neural networks"],
    "related_terms": ["StandardScaler", "Standardization", "MinMaxScaler"],
    "real_world_example": "```python\nimport numpy as np\n\n# Scale pixel intensities from [0, 255] to [0.0, 1.0]\nnormalized_img = image.astype(np.float32) / 255.0\n```",
    "difficulty_level": "Beginner",
    "term_type": "Technique"
  },
  {
    "id": "batch_expansion",
    "title": "Batch Dimension Expansion",
    "primary_category": "Computer Vision",
    "aliases": ["Batch dimension expansion for inference", "Batch dimension expansion", "unsqueeze"],
    "secondary_tags": ["Preprocessing", "Tensor Manipulation"],
    "definition": "Adding an extra dimension (axis) of size 1 to a tensor (e.g. changing shape from [C, H, W] to [1, C, H, W]) to match the batched shape expected by deep learning models.",
    "why_used": "Neural networks are designed to process batches of inputs concurrently. A single sample must be formatted as a 'batch of size 1' to satisfy the input layer dimensions.",
    "interview_point": "Models in PyTorch and TensorFlow expect tensors of shape `(Batch, Channels, Height, Width)`. For a single image, PyTorch developers use `tensor.unsqueeze(0)` or NumPy's `np.expand_dims(array, axis=0)` to prepend the batch dimension.",
    "advantages": ["Aligns input dimensions with batch processing layers", "Avoids framework dimension mismatch crashes"],
    "limitations": [""],
    "use_cases": ["Inference on single images in production"],
    "related_terms": ["Tensor Shape", "unsqueeze", "expand_dims"],
    "real_world_example": "```python\nimport torch\n\n# image_tensor has shape [3, 224, 224] (C, H, W)\nbatched_tensor = image_tensor.unsqueeze(0)  # Shape becomes [1, 3, 224, 224]\n```",
    "difficulty_level": "Beginner",
    "term_type": "Technique"
  }
]
